Linode API Javascript Bindings

linode.api.js -- Base class for accessing the API, works with jQuery or node.js
  instantiate with: new LinodeApi('yourapikey')
  usage: api.<api_method>({parameter1:, parameter2}, callback)
  method names have . changed to _ i.e. linode.list is really linode_list
  the callback takes one parameter, the results from the jquery call

generate.js -- is a node.js script to automatically generate linode.api.js
  in case linode makes changes to the api
